import getImages from '../api.js';

describe('getImages', () => {
    test('fetches images successfully', () => {

    });

    test('deliberately false call returns failure status code', () => {

    });

    test('fetches required number of images only', () => {

    });

    test('retrieved images match search term used', () => {

    });
})